"""
Compute gap = internal_confidence - displayed_confidence, normalize, assign labels.

Labels: overconfident (gap < 0), cowardly (gap > 0), calibrated (gap ≈ 0).
"""

# Stub for future use when using generated + scored pipeline
